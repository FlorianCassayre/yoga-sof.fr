import { InternalLink } from '../src/components/contents/common/InternalLink'; import {
  CourseRegistrationFormWidget
} from '../src/components/CourseRegistrationFormWidget';
import {
  FrontsiteContent
} from '../src/components/layout/public/FrontsiteContent';
import { EMAIL_CONTACT, FACEBOOK_PAGE_URL } from '../src/common/config';
import { Link as MuiLink } from '@mui/material';

export const meta = {
  title: 'Inscription à des séances de Yoga',
  description: `Inscriptions en ligne aux séances de Yoga adulte pour réserver sa place en petit groupe (maximum 5 personnes).`,
};

* Les inscriptions aux séances de Yoga adulte se font via le formulaire ci-dessous.<br />
Pour une inscription à des séances de Yoga enfant et Yoga parent-enfant,
veuillez nous <MuiLink href={`mailto:${EMAIL_CONTACT}`} target="_blank" rel="noopener noreferrer">envoyer un e-mail</MuiLink> ou
nous écrire depuis <MuiLink href={FACEBOOK_PAGE_URL} target="_blank" rel="noreferrer nofollow">notre page Facebook</MuiLink>.
* La première séance vous est proposée à 10 € à la date de votre choix. Ne programmez que celle-ci pour débuter.
* Dès que vous aurez validé le formulaire, vos inscriptions apparaîtront sur
<MuiLink href="/mes-inscriptions" target="_blank">votre page personnelle</MuiLink>.
* Vous reconnaissez avoir pris connaissance du <MuiLink href="/reglement-interieur" target="_blank">règlement intérieur</MuiLink>.
Vos données sont traitées selon notre <MuiLink href="/confidentialite" target="_blank">politique de confidentialité</MuiLink>.

<CourseRegistrationFormWidget />

export const RegistrationNoticeRecap = () => (
  <>
   Ce montant dépend de votre régularité aux cours (de 12€ ou 13€ par lot à 15€ à l’unité) et n’inclut pas la cotisation annuelle à l’association Yoga-Sof de 15€. Il est à régler à l’avance, de préférence via la billetterie en ligne par carte bancaire (me demander le lien) ou bien en liquide sur place.
  </>
);

export const RegistrationNoticePersonalInformation = ({ self }) => self ? (
  <>
    À compléter ou corriger au besoin, votre adresse e-mail est nécessaire pour communiquer avec vous.
  </>
) : (
  <>
    À compléter ou corriger au besoin. L'adresse email du proche est facultative.
  </>
);

export default ({ children }) => <FrontsiteContent {...meta}>{children}</FrontsiteContent>
