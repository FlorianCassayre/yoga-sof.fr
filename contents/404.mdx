import { FrontsiteContent } from '../src/components/layout/public/FrontsiteContent'; import {
    InternalLink
} from '../src/components/contents/common/InternalLink';

export const meta = {
  title: 'Page introuvable',
};

Si vous pensez qu'il s'agit d'une erreur, merci de nous le faire savoir.

<InternalLink href="/">Retourner à l'accueil</InternalLink>

export default ({ children }) => <FrontsiteContent {...meta}>{children}</FrontsiteContent>
